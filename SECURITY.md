# Security

Do not commit credentials, tokens, raw process-instance payloads, customer identifiers, or internal Camunda endpoint URLs.

This repository uses synthetic fixtures only. If adapting it to a live estate, aggregate process data before export and remove case-level identifiers.
